meta:
  environment: node
  toolchain:
    name: npm
    version: 20
build:
  command: npm install --build-from-source=sqlite3
run:
  command: npm start
  env:
    - name: DATA_DIR
      value: /data
